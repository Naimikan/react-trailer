---
name: Components
route: /components
---

import { Props } from 'docz';

import Code from '../../docz/code';

import VideoPlayer from '../../src';

# Components
---

## `VideoPlayer`

`VideoPlayer` is the component that comunicate all the components with the HTML5 video API. Internally, it's a provider.

<Code>
  {`
import VideoPlayer from 'react-trailer';\n
<VideoPlayer>
  <VideoPlayer.Viewer
    url="http://dl5.webmfiles.org/big-buck-bunny_trailer.webm"
    nativeControls
  />
</VideoPlayer>
  `.trim()}
</Code>

### Properties

<Props of={VideoPlayer} />

## `VideoPlayer.Viewer`
