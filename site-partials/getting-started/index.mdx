---
name: Getting Started
route: /
---

import { Playground } from 'docz';

import { VideoPlayer, Viewer } from '../../src/index';
import VersionTag from '../../docz/version-tag/index';

import { name, version } from '../../package.json';

# react-trailer <VersionTag version={version} name={name} />

react-trailer is a wrapper around HTML5 video API that allows you to render videos in your React application.

## Installation

To add the library to your project you can either use a CDN service:

```html
<script src="https://unpkg.com/react-trailer@1.2.1/index.js"></script>
```

Or npm/yarn to install the library from npm registry:

```bash
# With npm:
npm install --save react-trailer

# With yarn:
yarn add react-trailer
```

## Demo

<VideoPlayer>
  <Viewer
    url="https://d2zihajmogu5jn.cloudfront.net/elephantsdream/ed_hd.mp4"
    nativeControls
  />
</VideoPlayer>

## Usage

```jsx
import { VideoPlayer, Viewer } from 'react-trailer';

<VideoPlayer>
  <Viewer
    url="http://dl5.webmfiles.org/big-buck-bunny_trailer.webm"
    nativeControls
  />
</VideoPlayer>
```

## Development

```bash
npm i && npm start
```