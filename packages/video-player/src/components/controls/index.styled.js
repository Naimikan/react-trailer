import styled from 'styled-components';

const Controls = styled.div.attrs({
  className: 'rtr-player__controls',
})`
  position: absolute;
  width: 100%;
  bottom: 0;
  left: 0;
`;

export default Controls;
